<Window x:Class="Server.UtilityWindows.ReverseShell" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:header="clr-namespace:Server.UI.CustomControls.WindowHeaders"
        xmlns:helpers="clr-namespace:Server.UI.Style.Common.CommonHelper"
        Title="Reverse Shell - Fusion" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display" WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        MinWidth="300" MinHeight="200" Width="980" Height="515" Foreground="#CCCCCC" FontFamily="Consolas" FontSize="15" FontWeight="Medium">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="2.2" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>

    <Border Background="#0C0C0C" BorderThickness="1" BorderBrush="{StaticResource Border}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="27"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TitleBar -->
            <header:WindowHeader Background="{StaticResource Primary}"/>

            <!-- Main Content -->
            <Grid Grid.Row="2" Margin="10">
                
            </Grid>
        </Grid>
    </Border>
</Window>
